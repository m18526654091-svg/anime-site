# Next.js public site URL (used for sitemap, SEO, Open Graph)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Backend API URL
# - Server-side (SSR/SSG): used for direct FastAPI calls
# - Client-side: browser requests use same-origin /api proxy
NEXT_PUBLIC_API_URL=http://localhost:8000
